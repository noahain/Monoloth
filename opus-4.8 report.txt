{
  "summary": "Exhaustive bug audit of the Monolith Tauri app: Rust backend + vanilla JS frontend, with adversarial verification",
  "agentCount": 75,
  "logs": [
    "Collected 33 candidate findings across 8 finders",
    "Verified: 24 confirmed, 9 rejected of 33"
  ],
  "result": {
    "confirmedCount": 24,
    "rejectedCount": 9,
    "totalCandidates": 33,
    "synthesis": {
      "summary": "22 distinct bugs survived adversarial verification (24 raw findings, deduped to 22): 3 medium and 19 low severity. None are crashes reachable through normal in-app use; the one true crash (history.rs startup panic) requires an externally corrupted file. The findings cluster in five subsystems with recurring root causes worth fixing structurally rather than one-by-one:\n\n1. Window-state persistence (lib.rs): the three WindowEvent handlers (Resized/Moved/CloseRequested) apply inconsistent guards and size sources — Resized lacks the sentinel/bounds check the others have, and saves inner size while CloseRequested saves outer size. Fix by making all three handlers use the same sentinel/bounds guard and the same (inner) size source as the restore path.\n\n2. Config atomicity & consistency (config.rs / commands/config.rs): set_background_config does up to 8 independent locked writes (non-atomic + 8x disk writes); get() and get_all() can disagree for migrated global keys; delete_profile mutates in-memory/global state before the file delete, leaving a half-deleted state on failure. Root theme: read-modify-write operations that should be transactional are not.\n\n3. Terminal session-generation bookkeeping (pty.rs / terminal.js / sidebar.js / app.js): per-panel-tab generation entries are created but never reclaimed on tab close — in BOTH the Rust HashMap and the JS object — because panel-tab-N ids are monotonic and never reused. A related showTerminal race lets the frontend counter briefly run ahead of the backend on rapid relaunches.\n\n4. Inline-dialog / modal lifecycle (dialog.js / dom-utils.js): module-level singletons are used for inherently stackable/concurrent state — a single _idResolve/_idReject pair (strands the first promise when dialogs overlap) and a single focus-trap/saved-focus (a stacked modal loses its trap when an inner dialog closes). Both need per-instance/stack state.\n\n5. Updater footer & stall UI (updater-toast.js): a namespace typo (MonolothUI.showStatus, which never exists — the helper is on MonolothApp) silently degrades every footer update status to unstyled, non-auto-clearing text; the stall 'Cancel' button binds to the wrong (hidden) DOM element.\n\nCross-cutting root causes: (a) module-level singletons for stackable state, (b) per-session map entries created but never reclaimed, (c) inconsistent guards/lookups across sibling code paths (window handlers, image-ext substring vs array, namespace spelling). Highest-priority single fix is the history.rs char-boundary panic since it aborts app startup. Two findings (context-menu orphaned listeners, auto-theme stale brightness) carry only 1/2 verifier votes and are flagged as lower confidence.",
      "deduped": [
        {
          "title": "Non-char-boundary byte slicing in parse_iso_to_epoch panics on multibyte timestamps, aborting startup",
          "file": "F:/Coding Projects/Monolith/src-tauri/src/history.rs",
          "location": "parse_iso_to_epoch, lines 304-313 (len guard at 306)",
          "severity": "medium",
          "category": "panic",
          "description": "parse_iso_to_epoch indexes the timestamp by fixed BYTE ranges (s[0..4], s[5..7], ... s[17..19]) with only a byte-length guard (s.len() < 20). start_time/end_time are arbitrary strings deserialized from history.json, so a >=20-byte value containing a multibyte char near the start (e.g. \"cafe...\" with an accented e at bytes 3-4) makes s[0..4] cut mid-char and panics. HistoryManager::new() -> purge_inner() -> apply_retention() runs this over every session during app setup, and get_history_data calls it too.",
          "impact": "App crashes. A single malformed timestamp aborts startup (HistoryManager::new runs in setup); get_history_data panics out of the command. Only reachable via an externally corrupted/hand-edited history.json (the app only ever writes ASCII ISO), so it is a robustness/DoS-on-startup defect, not normally in-app triggerable.",
          "fix": "After the len check add `if !s.is_ascii() { return Err(()); }`, or replace each slice with char-boundary-safe `s.get(a..b).ok_or(())?`. Either makes a malformed entry fail gracefully instead of aborting setup."
        },
        {
          "title": "Nested modal close removes the focus trap from the still-open parent modal",
          "file": "F:/Coding Projects/Monolith/frontend/lib/dom-utils.js",
          "location": "trapFocus/releaseFocus/openModal/closeModal, lines 4-86",
          "severity": "medium",
          "category": "correctness",
          "description": "Focus trapping uses a single module-level _trapHandler and a single _focusBeforeModal. Opening a second modal removes the first's trap and overwrites _focusBeforeModal via saveFocus(). When the inner modal closes, releaseFocus() removes the trap entirely and restores the clobbered focus, leaving the still-open outer modal with NO trap and the wrong saved-focus element. Reachable via Settings > Profiles > New/Rename/Delete (opens an inline dialog over Settings) or Ctrl+P (command palette opens unconditionally over Settings at app.js:1426).",
          "impact": "Keyboard focus (Tab) leaks out of the visible modal into background controls; restoreFocus returns focus to the wrong element. Accessibility/keyboard-navigation regression for any stacked-modal scenario.",
          "fix": "Replace the single _trapHandler/_focusBeforeModal globals with a stack of {handler, savedFocus}. openModal pushes+installs a new trap; closeModal removes the current trap, pops, and re-installs the previous entry's handler + restores its savedFocus, only clearing fully when the stack empties. Note showSettings/hideSettings call saveFocus/trapFocus/releaseFocus directly (not via openModal/closeModal), so drive the stack consistently through those primitives or migrate Settings to openModal/closeModal so stacking balances."
        },
        {
          "title": "Updater footer status calls window.MonolothUI.showStatus, which never exists (namespace typo: it lives on MonolothApp)",
          "file": "F:/Coding Projects/Monolith/frontend/lib/updater-toast.js",
          "location": "checkFromFooter(), lines 393-394, 409-410, 422-423, 428-429, 440-441",
          "severity": "medium",
          "category": "logic-error",
          "description": "checkFromFooter() guards on `window.MonolothUI && window.MonolothUI.showStatus` and calls window.MonolothUI.showStatus('updater-status', ...). But showStatus is defined in app.js and exposed on window.MonolothApp.showStatus; the dom-utils.js MonolothUI export (lines 155-173) has no showStatus member. So the guard is always false and every branch falls through to the bare `else if (status) status.textContent = ...` path.",
          "impact": "The footer updater status (Checking, timed out, up to date, available, error) never gets success/error styling, the dismissible class, enter/exit animation, or the 6s auto-clear. Error messages render as plain (non-red) text and stick permanently with no auto-dismiss. The intended UI silently never runs in production.",
          "fix": "Use `window.MonolothApp && window.MonolothApp.showStatus` (facade is MonolothApp), or add a showStatus passthrough to the MonolothUI export in dom-utils.js. showStatus targets element id 'updater-status', which exists, so it works once wired. Update the test stub at updater-toast.test.cjs:235 to stub the real namespace (MonolothApp) so the test reflects production."
        },
        {
          "title": "set_background_config does up to 8 separate locked writes; non-atomic and 8x redundant disk writes per change",
          "file": "F:/Coding Projects/Monolith/src-tauri/src/commands/config.rs",
          "location": "set_background_config, lines 32-45 (NaN early-return for bg_transparency at 37-39)",
          "severity": "low",
          "category": "race-condition",
          "description": "Each config.set(...) acquires/releases the Arc<Mutex<ConfigInner>> separately and rewrites config.json on every call, so a logically-atomic background update is a sequence of independent locked writes. A concurrent get_all_config / window-save path can observe an intermediate state (e.g. bg_type=image but bg_image/bg_color still old). The NaN early-return for bg_transparency sits mid-sequence, so it can also leave a partial update persisted.",
          "impact": "Possible flash of an inconsistent background and 8x redundant disk writes per change; a partial state can be persisted if the app closes mid-sequence. Observable flash is an edge case (each file write is atomic via tmp+rename, same-command dispatch usually serializes, and the frontend paints from local state); the consistent win is eliminating the 7 redundant writes and making the update transactional.",
          "fix": "Validate first (keep/move the NaN check before building the pairs), collect changed pairs into a Vec<(&str, Value)>, and call config.set_many(&pairs) once so the whole update applies under one lock and one file write."
        },
        {
          "title": "Image-preview extension test uses substring indexOf on a concatenated blob, false-positives on short extensions",
          "file": "F:/Coding Projects/Monolith/frontend/file-picker.js",
          "location": "showPreview, line 467",
          "severity": "low",
          "category": "logic-error",
          "description": "The preview gate uses `'.png.jpg.jpeg.gif.bmp.webp.svg.ico'.indexOf(ext) === -1` instead of the FILE_IMAGE_EXTS array used by getItemIcon. indexOf does substring matching, so any ext that is a substring of the blob passes (e.g. '.j' matches inside '.jpg', '.p' inside '.png', '.we' inside '.webp'); an empty ext also passes since indexOf('')===0.",
          "impact": "Non-image files (data.j, x.p, etc.) are treated as images: the preview pane flashes open and get_file_preview is invoked needlessly, then shows 'Preview not available'. Inconsistent with the icon classification.",
          "fix": "Replace with `if (FILE_IMAGE_EXTS.indexOf(ext) === -1) { ... return; }` for exact-extension matching. This fixes the substring false-positives and the empty-extension case and aligns showPreview with getItemIcon."
        },
        {
          "title": "Cancelling the Close Tab confirm dialog produces an unhandled promise rejection",
          "file": "F:/Coding Projects/Monolith/frontend/sidebar.js",
          "location": "closeTab(), lines 628-636",
          "severity": "low",
          "category": "unhandled-promise-rejection",
          "description": "window.MonolithDialog.showConfirm() rejects on every cancel path (Cancel/X/overlay/Escape all run _idReject(new Error('cancelled'))). The close-tab caller attaches only .then() and no .catch(), so dismissing the 'This tab has a running process. Close anyway?' confirm leaves the rejection unhandled. The `if (confirmed)` guard is also dead (showConfirm only ever resolves true) but harmless.",
          "impact": "Unhandled rejection fires on every cancel — console noise and triggers any window 'unhandledrejection' / error-reporting listeners; in strict environments treated as uncaught. Not a crash (tab correctly stays open), but every other showConfirm caller avoids it.",
          "fix": "Add `.catch(function () {})` to the chain at sidebar.js:633, matching the other callers (app.js:1403/1504/1765/1823)."
        },
        {
          "title": "Clicking a still-initializing tab is a silent no-op; visual active state never switches",
          "file": "F:/Coding Projects/Monolith/frontend/sidebar.js",
          "location": "activateTab, lines 449-488 (guard at 456-458)",
          "severity": "low",
          "category": "logic-error",
          "description": "activateTab returns early when tab.initializing is true (`if (tab.initializing) return tab.initPromise || Promise.resolve();`) BEFORE any visual activation (deactivating old tab, adding .active, setting _activeTabId). The guard exists to avoid double-init but also swallows legitimate re-activation. Repro: create tab B (start_terminal pending), click A (becomes active), then click B again while it still initializes — activateTab(B) hits the guard and returns.",
          "impact": "The click does nothing: B's container stays hidden, the tab item is not highlighted, _activeTabId stays on A. The panel appears stuck on the wrong tab until init finishes and the user clicks again. Window is narrow (slow start_terminal IPC) and self-recovers.",
          "fix": "Run the visual switch first (deactivate old, add active classes, set _activeTabId, updateBusyDot), then place `if (tab.initializing) return tab.initPromise || Promise.resolve();` AFTER line 480 but BEFORE the `if (!tab.term)` block (482), so the DOM/state switch happens while still avoiding a duplicate initTabXterm/start_terminal call."
        },
        {
          "title": "Tooltip _activeEls entries leak (and tooltips get stuck) when profile rows are re-rendered",
          "file": "F:/Coding Projects/Monolith/frontend/tooltip.js",
          "location": "attach (82-105), cleanup (143-153); caller profiles.js:38,99",
          "severity": "low",
          "category": "resource-leak",
          "description": "attach() pushes {el,enter,leave,move} onto _activeEls without deduping against removed nodes. renderProfilesList does list.innerHTML='' then re-creates rows and re-attaches a tooltip per new delete button; the removed buttons' entries (with listeners) stay in _activeEls. cleanup() is the only pruner and is invoked only from sidebar.js, never after a profile re-render. If a delete button is hovered during re-render its node is removed before mouseleave, so hide() never runs.",
          "impact": "Unbounded growth of _activeEls (listeners on detached nodes) over a session; occasional orphaned tooltip stuck on screen with a dangling _tooltipTarget.",
          "fix": "In renderProfilesList, call window.MonolothTooltip.cleanup() after the loop (it prunes detached els), or switch profiles.js to deleteBtn.dataset.tooltip + MonolothTooltip.scan(list) like sidebar (idempotent re-attach via isAttached()). Also in cleanup(): when a pruned entry.el === _tooltipTarget, call hide()/clearTimer() so the pending 500ms timer cannot reveal a tooltip on a detached node."
        },
        {
          "title": "Shared _idResolve/_idReject module vars strand a dialog promise when two inline dialogs overlap",
          "file": "F:/Coding Projects/Monolith/frontend/dialog.js",
          "location": "_idResolve/_idReject module vars (lines 18-19); reassigned in showPrompt (~41-44) and showConfirm (~82-88)",
          "severity": "low",
          "category": "logic-error",
          "description": "Independently confirmed by two origins (js-app-core, js-theme-ui). The active dialog's resolve/reject live in single module-level vars and share one #inline-dialog element across showPrompt/showConfirm. Opening a second dialog before the first settles overwrites the callbacks, so the first promise never resolves or rejects.",
          "impact": "The earlier dialog's promise hangs forever; its .then/.catch chain (profile create/rename/delete flows) silently never runs. Latent — UI normally serializes inline dialogs.",
          "fix": "At the top of showPrompt/showConfirm, settle any in-flight dialog before reassigning: if isDialogActive(), swap _idResolve/_idReject to noop, then call the captured old _idReject(new Error('superseded')). Better: capture resolve/reject in a per-call closure and have closeDialog operate on the active instance instead of shared module state. Consumers already attach .catch, so rejecting a superseded dialog is safe."
        },
        {
          "title": "delete_profile mutates in-memory/global config to Default before the file delete, leaving inconsistent state on remove failure",
          "file": "F:/Coding Projects/Monolith/src-tauri/src/config.rs",
          "location": "AppConfig::delete_profile, lines 332-348",
          "severity": "low",
          "category": "logic-error",
          "description": "When deleting the active profile, delete_profile first switches active_profile to Default, rewrites config.json, and clears profile_overrides, then drops the lock and calls fs::remove_file. If remove_file fails (locked/read-only, e.g. open in an editor or AV scan), it returns Err but the user has already been switched to Default and the profile file still exists on disk (and reappears in list_profiles).",
          "impact": "User sees an error but is silently moved off their profile, the profile still exists, and the session's override data is lost from memory — a confusing half-deleted state.",
          "fix": "Reorder to match rename_profile: attempt fs::remove_file FIRST, and only after it succeeds acquire the lock and switch active_profile/global to Default and clear profile_overrides. No in-memory/global mutation happens unless the file delete succeeded."
        },
        {
          "title": "get() and get_all() disagree for a global key that exists in a stale profile override file (migration hazard)",
          "file": "F:/Coding Projects/Monolith/src-tauri/src/config.rs",
          "location": "AppConfig::get lines 217-229 vs AppConfig::get_all lines 256-265",
          "severity": "low",
          "category": "correctness",
          "description": "get() short-circuits global keys to inner.global and never consults profile_overrides; get_all() overlays ALL profile_overrides on top of global, including any that happen to be global keys. set_many never writes a global key into profile_overrides, so this is consistent in steady state. But if GLOBAL_KEYS gains a key that was previously profile-overridable (the list already has recent_directories, cmdPanelOpen, sidebar_config not in defaults()), an old profile file on disk can still contain it — then get(key) returns the global value while get_all()[key] returns the stale profile value.",
          "impact": "The frontend reads two different values for the same setting depending on whether it called get_config or get_all_config, causing inconsistent UI for migrated keys.",
          "fix": "In get_all(), skip overlaying any profile_overrides entry where is_global_key(k) is true. Optionally also strip global keys from profile_overrides at load (in new() and switch_profile()) to self-heal stale on-disk data permanently."
        },
        {
          "title": "Panel-tab session-generation entries leak on tab close; deleteSessionGeneration is now an unused export",
          "file": "F:/Coding Projects/Monolith/frontend/sidebar.js",
          "location": "_doCloseTab (sidebar.js:641-679); deleteSessionGeneration export at terminal.js:387; entries created via setSessionGeneration at sidebar.js:590-591",
          "severity": "low",
          "category": "resource-leak",
          "description": "Merges two findings sharing one fix. The uncommitted diff removed the only caller of window.MonolithTerminal.deleteSessionGeneration (was in app.js restartSession panel-tab branch ~1939), leaving zero callers. Separately, _doCloseTab never deletes the per-tab _sessionGeneration entry created when a panel tab's PTY starts. Because panel-tab-N ids are monotonic and never reused, entries are correctness-harmless but accumulate one per closed tab for the session lifetime. (_skipNextEof is already cleaned during restartSession and is never created for an open-then-closed tab, so it does not leak from close.)",
          "impact": "Slow unbounded growth of the _sessionGeneration object over a long session with heavy tab churn; deleteSessionGeneration is dead/misleading code implying a cleanup that never happens.",
          "fix": "Wire window.MonolithTerminal.deleteSessionGeneration(tab.sessionId) into _doCloseTab after _panelTabs.delete(tabId). This reclaims the leaked entries AND gives the orphaned export a real caller, resolving both observations together."
        },
        {
          "title": "Backend session_generation HashMap never reclaims retired panel-tab ids (Rust twin of the frontend leak)",
          "file": "F:/Coding Projects/Monolith/src-tauri/src/pty.rs",
          "location": "terminate, lines 236-249 (removes only from sessions); session_generation populated at 82-87",
          "severity": "low",
          "category": "resource-leak",
          "description": "spawn() inserts/increments a per-session_id counter in self.session_generation (82-87). terminate() removes the session only from self.sessions (239) and never touches session_generation; terminate_all() also only iterates sessions keys. Panel tabs use unique ids (panel-tab-<id>, see commands/terminal.rs:23), so every new tab adds a permanent session_generation entry that is never reclaimed. This is the backend counterpart of the frontend _sessionGeneration leak.",
          "impact": "Monotonic memory growth in the session_generation HashMap (small String+u64 per entry) over the process lifetime; never freed.",
          "fix": "Do NOT remove the entry in the restartable terminate() path — spawn() calls terminate() first (pty.rs:47) then relies on or_insert(0)+increment to keep generations monotonic across same-id restart (stale-EOF filtering). Reclaim only when an id is truly retired: clear session_generation in terminate_all(), or add a retirement command invoked when a panel tab is permanently closed (closeTab/_doCloseTab -> backend command removing both maps)."
        },
        {
          "title": "Stall-recovery 'Cancel' button is wired to the wrong (hidden) element, so it does nothing",
          "file": "F:/Coding Projects/Monolith/frontend/lib/updater-toast.js",
          "location": "handleStall(), lines 481-487 (querySelector at 482)",
          "severity": "low",
          "category": "logic-error",
          "description": "On stall, handleStall() injects an error block containing a new .update-toast-actions with .update-toast-retry and .update-toast-cancel, then resolves cancel with state.mounted.querySelector('.update-toast-cancel') — scoped to the whole toast. buildToastHtml (83-84) already put a .update-toast-cancel earlier in the DOM, so querySelector returns THAT first (hidden) button; the visible stall 'Cancel' gets no handler.",
          "impact": "Clicking Cancel in the stall UI does nothing (handler bound to the hidden main-actions cancel button). Trigger is a rare 2-minute stall. The header X (wired in wireToastEvents to removeMounted -> abortController.abort + toast removal) still works, so the user has a working redundant escape; the defect is a dead named control.",
          "fix": "Change line 482 to `var cancelBtn = errorEl.querySelector('.update-toast-cancel');` (and line 481 to errorEl.querySelector('.update-toast-retry') for consistency), matching the scoping in showErrorInToast."
        },
        {
          "title": "showTerminal bumps the main session generation unconditionally but skips spawn on a stale launchToken, briefly dropping the new session's earliest output",
          "file": "F:/Coding Projects/Monolith/frontend/app.js",
          "location": "showTerminal, lines 1315-1355 (increment at 1326, launchToken early-return at 1344)",
          "severity": "low",
          "category": "race-condition",
          "description": "showTerminal increments _sessionGeneration['main'] synchronously on every call (1326), but the actual spawn (initTerminal) runs in terminatePromise.finally and is skipped when launchToken !== _launchToken (1344). On rapid relaunches, each superseded call still increments the frontend generation without a backend spawn, so the frontend counter runs ahead of the backend's session_generation. When the final start_terminal resolves it resets the counter down; during the IPC window between the new PTY's first output (tagged with the lower backend generation) and that reset, the guard at terminal.js:318 ('generation < _sessionGeneration[sessionId]') can drop the new session's earliest output. PRE-EXISTING (1326 not in the diff); the diff's increments in refresh (1776) and restartSession main (1907) are unaffected since their finally always calls initTerminal.",
          "impact": "The first chunk of the new terminal's output (initial prompt/banner) can be silently dropped for one start_terminal IPC round-trip on 2+ rapid launches (e.g. double/triple-click), then self-heals. Rare and visually minor.",
          "fix": "Move `window.MonolithTerminal.incrementSessionGeneration('main')` from 1326 into the launchToken-guarded finally, right before initTerminal (after the `if (launchToken !== _launchToken) return;` check), so only the surviving call bumps the frontend generation and it stays in lockstep with the backend per-spawn increment and the post-resolve reset at terminal.js:184."
        },
        {
          "title": "Resized window handler persists position with no sentinel/bounds guard, unlike Moved and CloseRequested",
          "file": "F:/Coding Projects/Monolith/src-tauri/src/lib.rs",
          "location": "on_window_event WindowEvent::Resized, lines 156-159",
          "severity": "low",
          "category": "logic-error",
          "description": "After the debounce, the Resized branch saves outer_position with no validation. Moved (168-170) rejects positions <= WINDOW_MINIMIZED_SENTINEL (-32000) and CloseRequested (187) guards with > WINDOW_MINIMIZED_SENTINEL, but Resized applies neither the sentinel check nor the MIN/MAX_WINDOW_POSITION bounds. The only protection is the earlier is_minimized() check, which can race the OS reporting a minimized/off-screen position before is_minimized() flips.",
          "impact": "A bogus position (e.g. -32000) can be written to config during a minimize/restore animation or multi-monitor disconnect. sanitize_window_state self-heals it on next load, so the practical effect is a lost/reset position rather than an off-screen window, but the inconsistency with the other two handlers is a real defect.",
          "fix": "In the Resized branch, before cfg_for_events.set_window_position(pos.x, pos.y) at 158, add the Moved/CloseRequested guard: only save when pos.x as i64 > WINDOW_MINIMIZED_SENTINEL && pos.y as i64 > WINDOW_MINIMIZED_SENTINEL (ideally also within MIN/MAX_WINDOW_POSITION)."
        },
        {
          "title": "Resized saves inner size while CloseRequested saves outer size, causing window drift with native decorations",
          "file": "F:/Coding Projects/Monolith/src-tauri/src/lib.rs",
          "location": "WindowEvent::Resized line 156 vs CloseRequested lines 191-194; restore via window.set_size at lib.rs:124",
          "severity": "low",
          "category": "correctness",
          "description": "Resized persists size.width/size.height (inner size) from the event; CloseRequested persists window_clone.outer_size(). These differ by decoration extents when use_custom_titlebar is false (native decorations). Restore uses window.set_size (inner size). Mixing inner-sourced and outer-sourced saves against a single inner-size restore means the persisted dimension is not consistently defined.",
          "impact": "Window grows/shrinks by the decoration height/width across save/restore cycles when native decorations are active. Minor and only in non-default titlebar mode.",
          "fix": "Use one consistent (inner) size source. Change CloseRequested at lib.rs:191 to save window_clone.inner_size() instead of outer_size(), so both save paths and the restore path all use content dimensions. Do not flip restore to outer-based (Tauri has no convenient set_outer_size)."
        },
        {
          "title": "Integer overflow in retention cutoff computation",
          "file": "F:/Coding Projects/Monolith/src-tauri/src/history.rs",
          "location": "apply_retention, line 252",
          "severity": "low",
          "category": "integer-overflow",
          "description": "cutoff = epoch_seconds() - days * 86400, where days comes from parse::<i64>() of the retention string. retention_days parses arbitrarily large values (e.g. \"99999999999999999d\"); days * 86400 overflows i64. set_retention takes an unvalidated frontend string.",
          "impact": "Debug builds panic; release builds wrap to a bogus (large-negative) cutoff that defeats retention. Bad value persists to history.json.",
          "fix": "Compute with saturating arithmetic: `let cutoff = epoch_seconds().saturating_sub(days.saturating_mul(86400));`, and clamp days in retention_days (reject/cap above a sane max like 36500) so the bogus value never persists."
        },
        {
          "title": "Unbounded read of SVG files in get_file_preview (potential OOM)",
          "file": "F:/Coding Projects/Monolith/src-tauri/src/commands/fs.rs",
          "location": "get_file_preview, lines 200-203",
          "severity": "low",
          "category": "resource-leak",
          "description": "The SVG branch reads the whole file via fs::read_to_string(&path) with no size cap, inconsistent with sibling branches: text files cap at 4096 bytes via file.take(4096) (217) and images enforce a 20MB cap in read_image_as_data_url (image.rs:16). A preview only ever needs a snippet.",
          "impact": "Loads the entire file into memory; a multi-GB file with a .svg extension can exhaust RAM and crash the app. Practical blast radius is limited since SVGs are normally small text and preview is user-initiated.",
          "fix": "Read the SVG through the bounded text path (fs::File::open + file.take(4096) + read_to_end with the UTF-8 valid_up_to handling), or add a metadata.len() check before reading like read_image_as_data_url. The 4096-byte cap matches existing preview behavior."
        },
        {
          "title": "analyze_image_brightness reports a fully-transparent image as brightness 0 (treated as dark)",
          "file": "F:/Coding Projects/Monolith/src-tauri/src/commands/image.rs",
          "location": "analyze_image_brightness, lines 46-63",
          "severity": "low",
          "category": "logic-error",
          "description": "Brightness is an alpha-weighted average of luminance. For a fully transparent image weight_sum stays 0 and the function returns Ok(0.0) — the same value as a genuinely black image — so the adaptive light/dark logic treats a transparent background as maximally dark rather than 'no signal'.",
          "impact": "Adaptive theme picks dark-mode handling for a transparent image whose effective background may be light, producing wrong contrast. Contrived edge case (fully-transparent wallpaper only).",
          "fix": "Change the signature to Result<Option<f64>, String> and return Ok(None) when weight_sum == 0; surface as null so the frontend treats it like the no-image case (app.js:785 setWallpaperBrightness(null) leaves the theme at its mode default, matching theme.js:42 null-handling). Returning Ok(0.5) is a weaker alternative that still asserts a value."
        },
        {
          "title": "Context-menu dismiss listeners can be orphaned by a rapid second right-click (1/2 verifier votes — lower confidence)",
          "file": "F:/Coding Projects/Monolith/frontend/sidebar.js",
          "location": "showTabContextMenu, cleanup() and setTimeout block, lines 825-861",
          "severity": "low",
          "category": "resource-leak",
          "description": "Document-level dismiss/keydown listeners are added inside setTimeout(...,0) but _activeContextMenuCleanup is set immediately. If a second right-click (or any cleanup) fires before the deferred timer runs, cleanup() iterates an empty dismissListeners array (nothing to remove) and nulls _activeContextMenuCleanup; the pending setTimeout still runs and attaches listeners to document after cleanup has been disassociated. Confirmed by only one of two verifiers.",
          "impact": "Stale global click/keydown listeners referencing an already-removed menu get attached to document. They self-heal on the next document click/keydown (handler runs its own cleanup), so impact is limited extra listeners until then.",
          "fix": "Add a boolean (e.g. cleaned) set inside cleanup(); have the setTimeout(0) body return early if cleaned is true before attaching listeners. Preserves the deferral (which avoids the triggering click self-dismissing the menu) while preventing orphaned attachment."
        },
        {
          "title": "Auto theme applied with stale wallpaper brightness before brightness is set (1/2 verifier votes — lower confidence)",
          "file": "F:/Coding Projects/Monolith/frontend/app.js",
          "location": "loadBackgroundConfig, lines 759-789 (theme.js applyTheme 36-51)",
          "severity": "low",
          "category": "logic-error",
          "description": "applyTheme(themeMode) is called at app.js:759 before setWallpaperBrightness(...) at 775/780. In 'auto' mode, theme.js:42 reads _wallpaperBrightness (initial null or stale from a prior load) to decide adaptive-light; color/gradient branches then set brightness and re-call applyTheme('auto'), while image type corrects asynchronously via analyzeWallpaperBrightness. Confirmed by only one of two verifiers.",
          "impact": "Brief incorrect light/dark flash until the second applyTheme corrects it; for image backgrounds the flash is longer (async correction).",
          "fix": "Targets only the image path (color/gradient are already corrected synchronously before paint — no real flash there, and image brightness is only knowable via the async analyze_image_brightness IPC). Either (a) defer the reveal until analyzeWallpaperBrightness resolves (make it part of bgPromise) so auto+image never paints the wrong theme, or (b) keep applyTheme neutral and apply adaptive-light only after the async result."
        }
      ],
      "gaps": [
        "src-tauri/src/commands/shell.rs not covered: shell/argument construction and any command-string interpolation should be checked for injection and quoting safety on Windows.",
        "Path-traversal / arbitrary-file-read surface in commands/fs.rs (get_file_preview, directory listing) not audited — whether the frontend can read files outside an intended root.",
        "image.rs decode-bomb handling beyond the 20MB byte cap: a small compressed file can decode to huge pixel dimensions; no width*height guard was reviewed.",
        "version.rs and the updater pubkey / latest.json fetch+verify path were not audited for signature-verification correctness or error handling.",
        "General Rust panic surface: only history.rs slicing was found — unwrap()/expect()/array indexing across commands/*.rs and config.rs deserialization paths were not exhaustively swept.",
        "Broader concurrency review of config.rs Arc<Mutex<ConfigInner>>: read-modify-write races across other commands (window save vs profile switch vs set_many) beyond set_background_config.",
        "PtySession Drop / thread and handle cleanup on terminate and on app shutdown — whether reader threads and OS handles are fully reclaimed (separate from the session_generation map leak).",
        "Frontend event-listener lifecycle generally: only tooltip and context-menu were examined; app.js/sidebar.js add many DOM and IPC event listeners whose removal on teardown/re-render was not audited.",
        "Async race coverage in app.js launch/refresh/restart paths is partial (showTerminal found) — refresh (1776) and other launchToken/generation interactions deserve another pass.",
        "Test coverage gap: only renderer-policy, plugin-updater, and updater-toast have tests. sidebar.js, file-picker.js, dialog.js, theme.js, tooltip.js, and dom-utils.js focus/modal logic are untested, so regressions in the modal-stack and dialog fixes won't be caught.",
        "Accessibility beyond the focus-trap finding: ARIA roles/labels on dynamically built modals/menus, and whether new animated/glass elements are wired into the prefers-reduced-motion / prefers-reduced-transparency / prefers-contrast overrides per CLAUDE.md.",
        "tauri-bridge.js response-wrapping correctness across ALL methods (the wrapped-response gotcha) — only a few methods were spot-referenced; other callers may read raw fields off wrapped responses.",
        "style.css (~7000 lines) token consistency: --modal-* and other tokens that must be defined in BOTH :root and light-mode/adaptive-light blocks were not systematically diffed for missing light-mode overrides."
      ]
    },
    "rejectedTitles": [
      {
        "title": "Duplicate/concurrent spawn for same session_id orphans the previous child process and leaks its reader thread",
        "file": "src-tauri/src/pty.rs",
        "why": "The claim hinges on the assertion that \"Tauri dispatches commands on a thread pool and does not serialize concurrent invocations of the same command,\" creating a race window between terminate() at pty.rs:47 and insert at pty.rs:102. That premise is false for this command. start_terminal is declared `#[tauri::command]` (commands/terminal.rs:10) with no `async` keyword and no `(async)` attribute — a plain synchronous command. Per Tauri 2 semantics, synchronous commands run on the main (event-loop) thread and are processed serially; only async commands or `#[tauri::command(async)]` run on the async runtime thread pool. So two near-simultaneous start_terminal('main' or 'panel-tab-N') invocations are queued and executed one fully-to-completion before the other. The second invocation's terminate() (pty.rs:47) therefore runs only after the first has already inserted; it removes the prior entry and calls s.child.kill() (pty.rs:242-243), and the dropped reader hits EOF. There is no concurrent insert that overwrites a live entry, so no orphaned child and no permanently-blocked reader thread. I confirmed the cited code exactly matches: terminate() at line 47, the unconditional insert at line 102, terminate() remove+kill at lines 236-249, and that PtySession holds Box<dyn ChildKiller + Send> whose Drop does not kill. The drop-doesn't-kill mechanism is technically correct, but it is unreachable: the terminate-first guard plus single-threaded command serialization means the map key is always absent at insert time. The frontend (terminal.js:176 for 'main', sidebar.js:579 for unique 'panel-tab-N' ids via _nextTabId++) also does not fire two concurrent spawns for the same id under normal flows; panel-tab session ids are monotonically unique, and 'main' restart paths terminate-then-init sequentially."
      },
      {
        "title": "NaN bg_transparency aborts set_background_config, silently dropping theme_mode/cta_button_style/bg_layer",
        "file": "F:/Coding Projects/Monolith/src-tauri/src/commands/config.rs",
        "why": "The cited snippet exists verbatim at config.rs:36-45 and, in isolation, the `return;` would skip the later theme_mode/cta_button_style/bg_layer writes. But the claim's trigger (\"Frontend calls set_background_config with a non-finite bg_transparency\") is impossible: the frontend never invokes this Rust command. The bridge's api.set_background_config (tauri-bridge.js:236-258) reimplements the operation as independent per-key invoke('set_config', ...) calls (lines 238-254), each running unconditionally. theme_mode/cta_button_style/bg_layer are written via their own separate set_config IPC calls, so a NaN transparency cannot drop them. A repo-wide grep finds no invoke('set_background_config') anywhere — only the Rust definition (config.rs:21), its registration (lib.rs:241), the bridge wrapper, and app.js:855 calling that wrapper. The Rust command is registered but unreachable from the only frontend caller. Furthermore, the frontend transparency value is parseInt(slider.value,10) (app.js:853); a NaN there serializes to JSON null over IPC, never reaching an f64 NaN branch. The described user-facing impact (half-applied config, theme/CTA change silently lost) therefore cannot manifest. It is a latent code smell in effectively dead code, not a reproducible bug."
      },
      {
        "title": "analyze_image_brightness decodes images with no size or dimension limit (decompression bomb / OOM)",
        "file": "F:/Coding Projects/Monolith/src-tauri/src/commands/image.rs",
        "why": "The evidence snippet is accurate: analyze_image_brightness (src-tauri/src/commands/image.rs:42-43) calls image::open(&path) then img.thumbnail(64,64), with no file-size or dimension pre-check, unlike read_image_as_data_url's 20MB MAX_IMAGE_SIZE guard. However, the claimed impact (OOM / app crash from a decompression bomb) is refuted by the image crate's built-in limits. Cargo.toml pins image = \"0.25\" (resolved 0.25.10). image::open() = ImageReader::open(path)?.decode() (dynimage.rs:1632). ImageReader is constructed with Limits::default() (image_reader_type.rs:93,105), and Limits::default() sets max_alloc = Some(512MiB) (limits.rs:50-56). Critically, decode() (image_reader_type.rs:311-322) runs `limits.reserve(decoder.total_bytes())?` BEFORE allocating the output buffer via DynamicImage::from_decoder; reserve() returns ImageError::Limits(InsufficientMemory) if the decoded size exceeds 512MiB (limits.rs:96-108). total_bytes() is derived from cheaply-read header dimensions. So a crafted PNG that would decode to a multi-GB buffer is rejected with an Err (surfaced as \"Cannot open image: ...\") instead of crashing. The decompression-bomb -> memory exhaustion -> crash scenario does not hold for the default-configured crate."
      },
      {
        "title": "History never capped by count; unparseable/unknown retention disables purging entirely",
        "file": "F:/Coding Projects/Monolith/src-tauri/src/history.rs",
        "why": "The cited code at history.rs:232-262 exists exactly as quoted. retention_days() returns None for any string not equal to 7d/30d/90d and not matching \"Nd\", and apply_retention() early-returns with no pruning in that case. The \"never purge\" path is reachable: frontend/index.html:487 exposes a `data-retention=\"forever\"` button, tauri-bridge.js:381 forwards it via set_history_retention, and \"forever\" does not end in 'd' so retention_days returns None. So the finder's facts are correct. However, this is intended behavior, not a bug. \"Forever\" is a deliberate user-facing retention choice, and never-purging is precisely what that option promises — selecting \"keep forever\" and getting unbounded retention is working as designed, not a control-flow defect. The only non-parseable value reachable from the UI is \"forever\"; arbitrary strings via set_retention are not exploitable beyond what the UI already offers. The lack of a hard max-session-count cap is a missing hardening/enhancement, not a defect: with any valid retention (7d/30d/90d) growth is bounded by the time window, and terminal session-end history is inherently low-volume, so the claimed performance degradation is theoretical. apply_retention does correctly drop entries with unparseable timestamps (None => false). I default to isReal=false because the behavior the finder flags is the documented, intentional semantics of the \"Forever\" option rather than a reproducible defect."
      },
      {
        "title": "restartSession('panel') never terminates the existing panel PTY because _panelRunning is never set true",
        "file": "frontend/app.js",
        "why": "The factual snippet is accurate: in F:\\Coding Projects\\Monolith\\frontend\\app.js, _panelRunning is declared false (line 20) and only ever assigned false (line 1394 backToLanding, line 1953 restart .finally); a full frontend grep confirms no `_panelRunning = true` exists, so `if (_panelRunning)` at line 1950 is dead. However, this is not a reproducible bug. The branch is only reached via restartSession('panel'), and no code path passes the literal 'panel': the sole real caller (sidebar.js:715) passes tab.sessionId, which is always 'panel-tab-'+_nextTabId (sidebar.js:383); the renderer-policy tests pass only 'main' and 'panel-tab-1'. The restartSession dispatcher handles 'main' and sessionId.startsWith('panel-tab-') for live sessions; the `else if (sessionId === 'panel')` block (app.js:1949) is itself unreachable in current code. Additionally, no 'panel'-id PTY is ever spawned by the frontend (CMD panel tabs use panel-tab-N), so there is no live session to orphan and no EOF to leak. The claimed IMPACT (orphaned backend session, mishandled EOF) therefore cannot occur. This is vestigial/dead code, not an active defect with observable misbehavior."
      },
      {
        "title": "Background tab steals keyboard focus 100ms after init if user switched away",
        "file": "F:/Coding Projects/Monolith/frontend/sidebar.js",
        "why": "The evidence snippet exists verbatim at sidebar.js:593-600 inside the start_terminal().then() success branch, with no active-tab guard, and the trigger is reachable (activateTab only short-circuits when the TARGET tab is initializing, line 456, so switching back to an already-running tab is allowed and strips .active from the new tab at lines 460-468). However the claimed impact does not occur. Inactive tab containers are display:none (style.css:7983-7993; only .active gets display:flex). When the user switches away, the new tab's container becomes display:none, so the term.focus() call hits xterm's helper textarea inside a non-rendered container. Browsers do not move focus to display:none elements — .focus() is a silent no-op and document.activeElement is unchanged — so focus is NOT yanked from the terminal the user is typing in, and keystrokes do not go to the background terminal. The 'bogus PTY size' concern also fails: the vendored FitAddon.fit() (lib/xterm-addon-fit.js) computes from the parent's computed width/height, which is NaN for a display:none parent, so fit() early-returns without resizing; the following resize_terminal sends term.cols/term.rows which retain the valid values measured at term.open() time (the container was .active when init began, line 389). So neither the focus-steal nor the PTY-corruption effect can happen. The display:none CSS effectively guards both."
      },
      {
        "title": "Image preview onload handler assigned after src is set",
        "file": "F:/Coding Projects/Monolith/frontend/file-picker.js",
        "why": "The cited snippet is accurate (file-picker.js:482-485): src is set, then onload is assigned on the next line. However, the claimed race is impossible under the browser/WebView2 (Chromium) event model. Image load events are always dispatched asynchronously as an event-loop task, never fired synchronously inside the `img.src` setter — this is true even for data: URLs that decode immediately. Lines 482-485 run inside a single synchronous block (the get_file_preview .then callback). The event loop cannot execute the queued load task until that synchronous block completes, by which point onload at line 485 is already attached. Therefore the .loaded class cannot be missed due to ordering. The finder is reasoning from a non-existent synchronous-dispatch model. (A distinct, plausible issue exists — re-selecting the identical dataUrl where Chromium may not re-fire load after .loaded was removed at line 477 — but that is not the timing race described, and it is not what was claimed or evidenced.)"
      },
      {
        "title": "Resize clamp order can drive panel height below the 80px minimum on small windows",
        "file": "F:/Coding Projects/Monolith/frontend/sidebar.js",
        "why": "The cited evidence is real: sidebar.js onResizeMouseMove (lines 918-922) clamps `if (newHeight < 80) newHeight = 80;` then `var maxH = window.innerHeight * 0.6; if (newHeight > maxH) newHeight = maxH;`, and applyPanelHeight (lines 368-371) does no further clamping. So in pure isolation the max clamp can override the 80px floor when maxH < 80. However, the trigger requires window.innerHeight < ~134px so that innerHeight*0.6 < 80. This is unreachable: tauri.conf.json sets the window minHeight to 500 (resizable true but floored at 500), and config.rs additionally enforces MIN_WINDOW_HEIGHT = 150 with sanitization on load. With a 500px floor, innerHeight*0.6 is always ~300px, far above 80. The max clamp therefore can never win against the min, so the panel height cannot be driven below 80px in any real configuration. The bug is theoretical only and not reproducible."
      },
      {
        "title": "renameProfileInline omits the filename-character validation that profile creation enforces",
        "file": "frontend/profiles.js",
        "why": "The claimed impact — a rename producing a path-illegal profile filename that corrupts persistence — is refuted by the backend. In F:\\Coding Projects\\Monolith\\src-tauri\\src\\config.rs, rename_profile (lines 350-373) calls validate_profile_name(new)? at line 355, identical to how create_profile (line 317) validates. validate_profile_name (lines 34-59) is in fact STRICTER than the frontend regex: it rejects /, \\, .., :, \\0, Windows reserved device names (CON, NUL, COM1, etc.), and permits only ASCII alphanumeric, space, hyphen, and underscore. So any illegal newName passed to rename_profile is rejected server-side, returns Err, and renameProfileInline (frontend/profiles.js lines 157-168) handles it: the bridge response has success=false, and the .then branch shows res.error || 'Failed to rename profile' via showStatus. No illegal filename is ever written to %APPDATA%/Monoloth/profiles/, so persistence cannot be corrupted. The evidence snippet is factually accurate (the frontend rename genuinely lacks the regex that add-profile has at line 178), but the consequence is purely cosmetic: a rename with bad characters surfaces the generic backend error message rather than the friendlier frontend message. That is a minor UX inconsistency, not a correctness bug."
      }
    ]
  }
}